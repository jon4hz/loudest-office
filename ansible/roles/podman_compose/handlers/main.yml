---
- name: Restart compose service
  ansible.builtin.systemd_service:
    name: compose.service
    scope: user
    daemon_reload: true
    state: restarted
  listen: restart_compose_service
  become: true
  become_user: "{{ podman_compose_name }}"
